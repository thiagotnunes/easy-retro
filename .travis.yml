language: ruby
rvm:
  - 1.9.2

# blacklist
branches:
  except:
    - spikes
    - experimental

# whitelist
#branches:
  #only:
    #- master

before_script:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
